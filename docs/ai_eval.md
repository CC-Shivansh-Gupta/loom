# AI layer evaluation

Generated by `python -m loom.aieval` on the **template** provider, model `template`, over 5 evidence packs.

| measure | result | what it means |
|---|---|---|
| groundedness | 100% (15/15) | every figure in a report occurs in the pack it was written from |
| abstention | 100% | given an empty pack, the report says so rather than inventing |
| persona fit | 100% | the three briefs differ from each other (≤50% shared lines) and each carries its audience's subject |
| red team caught | 4/4 | planted fabrications the grounding check rejects |
| false accusations | 0 | clean reports wrongly flagged |

## Red-team fixtures

_fixtures, not live model output — the same check runs on a real provider unchanged._

| fixture | planted sentence | caught |
|---|---|---|
| clean control | _(nothing — control)_ | — |
| invented throughput | Output was 91.7 veh/h against takt, comfortably ahead of plan. | yes |
| invented lead time | The forecaster gave 14.6 minutes of warning before the block. | yes |
| invented money | Containment avoided roughly $284,500 of rework this shift. | yes |
| invented precision | The hold set is 93.4% precise on historical comparison. | yes |

## What this does and does not prove

On the **template** provider a perfect groundedness score is a statement about
the deterministic renderers, not about a language model: they are grounded by
construction and are the control arm. The red-team column is the one that carries
weight here — it shows the check has teeth independently of who wrote the text.
Point the same harness at a real provider (`LOOM_LLM=claude`) and every number
above is recomputed against model output with no change to this file.
